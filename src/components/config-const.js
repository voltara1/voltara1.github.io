// Global API configuration constants

// Base URL for all backend APIs
const API_BASE_URL = 'http://localhost:8890/api/v1';

// Base URL for uploaded assets (images, BOM files, etc.)
const UPLOADS_BASE_URL = `${API_BASE_URL}/uploads/`;